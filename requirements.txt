# LipSense - Python dependencies
# Target: Python 3.10 (Windows 11). Newer Python has dlib/tensorflow issues.
#
# Install:
#   python -m venv venv
#   venv\Scripts\activate
#   pip install -r requirements.txt

# ---- Core deep learning ----
tensorflow==2.10.1          # last version with easy native Windows support (py3.10)
numpy==1.23.5               # tensorflow 2.10 needs numpy<1.24
scikit-learn==1.3.2
matplotlib==3.7.5
reportlab==5.0.1           # weekly Form-3 progress report PDF (generate_report.py)

# ---- Computer vision ----
opencv-python==4.8.1.78
imutils==0.5.4
dlib==20.0.1               # v20 ships a prebuilt Windows wheel (py3.10) - no build tools needed
# mediapipe==0.10.9        # dlib fallback (face mesh landmarks), if ever needed

# ---- Backend (later) ----
fastapi==0.110.0
uvicorn[standard]==0.29.0
websockets==12.0
python-multipart==0.0.9

# ---- Notebook / training ----
jupyter==1.0.0
notebook==7.1.2
pandas==2.0.3
seaborn==0.13.2
tqdm==4.66.2
